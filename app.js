const express=require('express'),path=require('path'),app=express();api_mail=require('./api/api_mail');api_tg_bot=require('./api/api_tg_bot');require('dotenv').config();app.use(express.static(path.join(__dirname,"/public")));const urlencodedParser=express.urlencoded({extended:!0});app.post("/req",urlencodedParser,(req,res)=>{let age_r=(req.body.age_r)[1]===''?(req.body.age_r)[0]:req.body.age_r;const request={age_r:age_r,count_r:req.body.count_r,time_r:req.body.time_r,date:req.body.inputDate,name:req.body.name,number:req.body.number};console.log(request);api_mail.sendToEmail(request).catch(console.error);api_tg_bot.sendToBot(request).catch(console.error);res.redirect("/")});app.listen(process.env.PORT,process.env.HOST,()=>{let s=process.env.HOST+':'+String(process.env.PORT);console.log("Server was dead before we've started on "+s)})